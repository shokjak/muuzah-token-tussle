# Devvit configuration
name: muuzah-game
version: 0.0.5

capabilities:
  - redis
  - redditAPI

# Webview configuration - nested format for proper asset resolution
post:
  dir: webroot
  entrypoints:
    default:
      entry: index.html
