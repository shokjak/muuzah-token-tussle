# Devvit configuration
name: muuzah-game
version: 0.0.7

capabilities:
  - redis
  - redditAPI

# Webview configuration - nested format as per DEVVIT_DEPLOYMENT.md
post:
  dir: webroot
  entrypoints:
    default:
      entry: index.html