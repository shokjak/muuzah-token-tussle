# Devvit configuration for Muuzah
name: muuzah-game
version: 0.0.3

capabilities:
  - redis
  - redditAPI

# Webview configuration - correct format
post:
  dir: webroot
  entrypoints:
    default:
      entry: index.html
